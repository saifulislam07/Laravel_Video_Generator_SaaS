# Laravel Video Generator SaaS — Claude Code Prompt Series

**প্রজেক্ট:** কার্টুন ক্যারেক্টার + রিয়েল ব্যাকগ্রাউন্ড কম্পোজিট ভিডিও জেনারেটর
**স্ট্যাক:** Laravel 13 + Livewire + Shotstack/Creatomate API
**অ্যাপ্রোচ:** Standalone-first, ফেজ ধরে ধরে ডেভেলপমেন্ট

প্রতিটা ফেজের প্রম্পট আলাদা Claude Code সেশনে (বা একই সেশনে ধারাবাহিকভাবে) পেস্ট করুন। প্রতি ফেজ শেষে টেস্ট করে পরের ফেজে যান — একসাথে সব প্রম্পট না দিয়ে ধাপে ধাপে দিলে ফলাফল অনেক পরিষ্কার হবে।

---

## Phase 0: প্রজেক্ট প্ল্যানিং ডকুমেন্ট

```
আমি একটা Laravel 13 SaaS বানাতে চাই যেটা ইউজারকে রিয়েল ব্যাকগ্রাউন্ড ছবির উপর
কার্টুন ক্যারেক্টার বসিয়ে ছোট ভিডিও (Reels স্টাইল, ৩০-৪৫ সেকেন্ড) বানাতে দেবে,
Facebook/Instagram-এ পোস্ট করার জন্য।

ভিডিও রেন্ডারিং নিজে FFmpeg দিয়ে না করে Shotstack (বা Creatomate) API ব্যবহার করব।

একটা PROJECT_PLAN.md ফাইল বানাও যেখানে থাকবে:
- প্রজেক্টের সংক্ষিপ্ত বর্ণনা ও মূল ফিচার লিস্ট
- টেক স্ট্যাক (Laravel 13, Livewire 3, Shotstack API, MySQL, Redis queue)
- ইউজার ফ্লো: সাইনআপ → ব্যাকগ্রাউন্ড আপলোড → ক্যারেক্টার সিলেক্ট →
  সংলাপ টেক্সট লেখা → রেন্ডার রিকোয়েস্ট → স্ট্যাটাস দেখা → ডাউনলোড
- একটা ফেজ-ভিত্তিক ডেভেলপমেন্ট রোডম্যাপ (এই প্রম্পট সিরিজের ফেজগুলো অনুসরণ করে)

এখনো কোনো কোড লিখো না, শুধু প্ল্যানিং ডকুমেন্ট বানাও।
```

---

## Phase 1: বেস প্রজেক্ট, অথ ও কিউ সেটআপ

```
Laravel 13 প্রজেক্ট সেটআপ করো এই কনফিগারেশন দিয়ে:

1. Laravel Breeze দিয়ে অথেন্টিকেশন সেট করো (Livewire স্ট্যাক ব্যবহার করে)
2. Queue driver হিসেবে database driver সেট করো config/queue.php-তে,
   এবং জব টেবিলের মাইগ্রেশন রান করো (queue, failed_jobs)
3. Laravel Horizon ইনস্টল করো যদি Redis ব্যবহারযোগ্য হয়,
   নাহলে database queue driver-এর জন্য একটা সহজ কিউ মনিটরিং সলিউশন সাজেস্ট করো
4. .env.example ফাইলে প্লেসহোল্ডার হিসেবে এই ভ্যারিয়েবলগুলো যোগ করো:
   SHOTSTACK_API_KEY, SHOTSTACK_ENV, SHOTSTACK_TEMPLATE_ID
5. একটা বেসিক ড্যাশবোর্ড লেআউট বানাও (AdminLTE বা Tailwind, তুমি ঠিক করো)
   যেখানে লগইন করার পর ইউজার তার প্রজেক্ট/ভিডিওগুলো দেখতে পাবে

কাজ শেষে একটা সংক্ষিপ্ত সামারি দাও কী কী সেটআপ হলো এবং পরবর্তী ধাপে কী করতে হবে।
```

---

## Phase 2: ডাটাবেজ স্কিমা ডিজাইন

```
নিচের এনটিটিগুলোর জন্য Laravel মাইগ্রেশন, মডেল ও রিলেশনশিপ বানাও:

1. **projects** — id, user_id, title, status (draft/rendering/completed/failed), created_at
2. **characters** — id, user_id (nullable, null হলে সিস্টেম-ডিফল্ট ক্যারেক্টার),
   name, thumbnail_path, is_public (boolean)
3. **character_poses** — id, character_id, pose_name (e.g. 'smiling', 'surprised'),
   image_path (transparent PNG)
4. **scenes** — id, project_id, order (sequence number), background_image_path,
   dialogue_text, duration_seconds
5. **scene_characters** — pivot টেবিল: scene_id, character_pose_id,
   position_x, position_y, scale
6. **video_renders** — id, project_id, shotstack_render_id, status
   (queued/rendering/done/failed), output_url, error_message, timestamps

সব মডেলে যথাযথ fillable, cast, ও Eloquent relationship (hasMany, belongsTo,
belongsToMany) সেট করো। প্রতিটা টেবিলে দরকারি ইনডেক্স যোগ করো
(user_id, project_id, status কলামে)।

মাইগ্রেশন রান করে কনফার্ম করো সব টেবিল ঠিকভাবে তৈরি হয়েছে।
```

---

## Phase 3: অ্যাসেট ম্যানেজমেন্ট (ব্যাকগ্রাউন্ড ও ক্যারেক্টার আপলোড)

```
একটা অ্যাসেট ম্যানেজমেন্ট মডিউল বানাও:

1. Livewire কম্পোনেন্ট যেখানে ইউজার ব্যাকগ্রাউন্ড ছবি আপলোড করতে পারবে
   (jpg/png, max 10MB, storage/app/public/backgrounds-এ সেভ হবে)
2. আপলোড করা ছবি অটো-রিসাইজ/অপটিমাইজ করার জন্য Intervention Image
   লাইব্রেরি ইনস্টল ও কনফিগার করো (max width 1080px রাখো, Reels ফরম্যাটের জন্য)
3. একটা সিডার বানাও যেটা ৩-৪টা ডিফল্ট সিস্টেম ক্যারেক্টার (placeholder PNG,
   পরে রিয়েল আর্ট দিয়ে বদলে দেব) ও তাদের বেসিক পোজ (idle, smiling, surprised)
   ডাটাবেজে যোগ করে
4. একটা গ্যালারি ভিউ বানাও যেখানে ইউজার তার আপলোড করা ব্যাকগ্রাউন্ড ও
   অ্যাভেইলেবল ক্যারেক্টারগুলো ব্রাউজ করতে পারবে

Storage link (php artisan storage:link) সেটআপ ঠিক আছে কিনা চেক করো।
```

---

## Phase 4: সিন বিল্ডার ইন্টারফেস

```
একটা "Scene Builder" Livewire কম্পোনেন্ট বানাও যেখানে ইউজার একটা প্রজেক্টের
ভেতরে একাধিক সিন যোগ করতে পারবে। প্রতিটা সিনে:

1. ব্যাকগ্রাউন্ড ছবি সিলেক্ট করা (আগের গ্যালারি থেকে)
2. একটা ক্যারেক্টার পোজ সিলেক্ট করা ও তার position (x, y) ও scale সেট করা
   — এটার জন্য একটা সিম্পল ড্র্যাগ-অ্যান্ড-ড্রপ প্রিভিউ বানাও (Alpine.js দিয়ে,
   ছবির উপর ক্যারেক্টার PNG ড্র্যাগ করে পজিশন ঠিক করা যাবে)
3. সংলাপ টেক্সট লেখার জন্য একটা টেক্সটএরিয়া (ক্যাপশন হিসেবে ব্যবহার হবে)
4. সিনের duration (সেকেন্ডে, ডিফল্ট 5) সেট করা

ইউজার একাধিক সিন যোগ করে একটা প্রজেক্টের সব সিন অর্ডার করতে পারবে
(drag to reorder)। প্রতিটা সিন সেভ হলে scenes ও scene_characters
টেবিলে ডাটা যাবে।

শেষে একটা "Preview Timeline" ভিউ বানাও যেখানে সব সিন ক্রমান্বয়ে
থাম্বনেইল আকারে দেখা যাবে।
```

---

## Phase 5: Shotstack টেমপ্লেট ও JSON payload বিল্ডার

```
একটা সার্ভিস ক্লাস বানাও: app/Services/ShotstackPayloadBuilder.php

এই ক্লাসের কাজ হবে একটা Project মডেল নিয়ে সেটাকে Shotstack API-র
প্রত্যাশিত JSON স্ট্রাকচারে রূপান্তর করা। Shotstack-এর Edit API ডকুমেন্টেশন
অনুযায়ী (timeline > tracks > clips স্ট্রাকচার):

1. প্রতিটা scene-কে একটা clip হিসেবে ট্রিট করো, যেখানে:
   - ব্যাকগ্রাউন্ড ইমেজ একটা track-এ (নিচের লেয়ার)
   - ক্যারেক্টার PNG আরেকটা track-এ (উপরের লেয়ার), সঠিক position/scale সহ
   - dialogue_text একটা title/caption asset হিসেবে (নিচে বা উপরে ওভারলে)
   - duration সেই সিনের duration_seconds অনুযায়ী
2. মেথডটা একটা array রিটার্ন করবে যেটা সরাসরি Shotstack API-তে POST
   করা যাবে
3. আউটপুট রেজোলিউশন 1080x1920 (9:16 vertical) সেট করো, যেহেতু এটা Reels-এর জন্য

Shotstack-এর অফিসিয়াল ডকুমেন্টেশন (https://shotstack.io/docs/) থেকে
সঠিক JSON স্কিমা রেফারেন্স করে বানাও। যদি ওয়েব সার্চ করার দরকার হয়,
সেটা করে নিশ্চিত হও স্ট্রাকচারটা সঠিক।

একটা টেস্ট (Pest/PHPUnit) লিখো যেটা একটা স্যাম্পল Project দিয়ে
এই বিল্ডার কল করে ভ্যালিড JSON স্ট্রাকচার পাওয়া যাচ্ছে কিনা যাচাই করে।
```

---

## Phase 6: রেন্ডার রিকোয়েস্ট, স্ট্যাটাস পোলিং ও Webhook

```
একটা VideoRenderService বানাও যেটা:

1. ShotstackPayloadBuilder ব্যবহার করে JSON payload বানায়
2. Laravel HTTP client দিয়ে (Http::withToken()) Shotstack API-তে
   POST /render রিকোয়েস্ট পাঠায়
3. রেসপন্স থেকে render_id নিয়ে video_renders টেবিলে status='queued'
   সহ একটা রেকর্ড সেভ করে

এরপর একটা Laravel Job বানাও: CheckRenderStatusJob
- এটা প্রতি ৩০ সেকেন্ডে render_id দিয়ে Shotstack-এর
  GET /render/{id} এন্ডপয়েন্ট চেক করবে
- status 'done' হলে output_url সেভ করে video_renders.status='completed' করবে
- status 'failed' হলে error message সেভ করবে
- এই জবটা নিজেই নিজেকে আবার ডিসপ্যাচ করবে (retry with delay) যতক্ষণ না
  render শেষ হয়, ম্যাক্স ১০ মিনিট পর্যন্ত ট্রাই করার পর টাইমআউট হিসেবে
  মার্ক করবে

শেষে Laravel Broadcasting (Laravel Reverb) সেটআপ করো যাতে রেন্ডার
স্ট্যাটাস আপডেট হলে ফ্রন্টএন্ডে (Livewire wire:poll বা Echo listener দিয়ে)
রিয়েল-টাইমে দেখা যায়। একটা ProjectRenderStatusUpdated ইভেন্ট বানাও
যেটা প্রতি স্ট্যাটাস চেঞ্জে broadcast হবে।
```

---

## Phase 7: ইউজার ড্যাশবোর্ড ও ভিডিও ডেলিভারি

```
একটা ইউজার ড্যাশবোর্ড বানাও যেখানে:

1. ইউজার তার সব প্রজেক্ট (draft, rendering, completed, failed status সহ)
   একটা লিস্টে দেখতে পারবে
2. প্রতিটা completed প্রজেক্টের জন্য একটা ভিডিও প্লেয়ার (HTML5 video tag)
   দেখাবে output_url থেকে, সাথে একটা ডাউনলোড বাটন
3. rendering স্ট্যাটাসে থাকলে একটা প্রোগ্রেস ইন্ডিকেটর/স্পিনার দেখাবে,
   Laravel Echo দিয়ে রিয়েল-টাইম আপডেট শুনবে
4. failed হলে error message ও একটা "আবার চেষ্টা করুন" বাটন থাকবে

ভিডিও ফাইলগুলো (Shotstack থেকে আসা .mp4 URL) কি লোকাল স্টোরেজে কপি
করে রাখা উচিত নাকি সরাসরি Shotstack-এর CDN URL ব্যবহার করা উচিত —
দুটোর প্রোজ কনস বলো এবং তোমার সাজেশন অনুযায়ী ইমপ্লিমেন্ট করো
(সাধারণত শুরুতে সরাসরি CDN URL ব্যবহার করাই সহজ ও সস্তা)।
```

---

## Phase 8: ক্রেডিট/বিলিং সিস্টেম (মনিটাইজেশনের জন্য)

```
যেহেতু প্রতিটা ভিডিও রেন্ডারে Shotstack API খরচ হয়, একটা ক্রেডিট সিস্টেম
বানাও যাতে ফ্রি টিয়ার ইউজারদের সীমিত রেন্ডার দেওয়া যায়:

1. users টেবিলে একটা credits (integer) কলাম যোগ করো, ডিফল্ট 5
   (নতুন ইউজার ৫টা ফ্রি ভিডিও পাবে)
2. প্রতিটা রেন্ডার রিকোয়েস্ট পাঠানোর আগে চেক করো ইউজারের ক্রেডিট আছে কিনা,
   না থাকলে একটা "Upgrade" মেসেজ দেখাও
3. সফল রেন্ডার শুরু হলে ১ ক্রেডিট কেটে নাও (একটা credit_transactions
   টেবিলে লগ রাখো — amount, reason, timestamp)
4. bKash/SSLCommerz ইন্টিগ্রেশনের একটা বেসিক স্ট্রাকচার বানাও যেখানে
   ইউজার প্যাকেজ কিনে ক্রেডিট রিচার্জ করতে পারবে (এখনো লাইভ পেমেন্ট
   গেটওয়ে কানেক্ট করো না, শুধু স্ট্রাকচার ও UI বানাও)

একটা প্রাইসিং পেজও বানাও যেখানে ৩টা প্যাকেজ (যেমন ১০/৫০/১০০ ভিডিও)
তাদের দাম সহ দেখানো থাকবে।
```

---

## Phase 9: অ্যাডমিন প্যানেল

```
একটা বেসিক অ্যাডমিন প্যানেল বানাও (AdminLTE ব্যবহার করে, তোমার আগের
Admin Kit প্যাটার্ন অনুসরণ করে) যেখানে অ্যাডমিন:

1. সব ইউজার ও তাদের ক্রেডিট ব্যালেন্স দেখতে ও ম্যানুয়ালি এডজাস্ট
   করতে পারবে
2. সিস্টেম ক্যারেক্টার (is_public=true) আপলোড/এডিট/ডিলিট করতে পারবে
3. সব রেন্ডার হওয়া ভিডিওর একটা লিস্ট দেখতে পারবে (মডারেশনের জন্য)
4. বেসিক স্ট্যাটিস্টিক্স ড্যাশবোর্ড (মোট ইউজার, মোট রেন্ডার, আজকের
   রেন্ডার সংখ্যা) দেখাবে

Laravel-এর role/permission-এর জন্য spatie/laravel-permission প্যাকেজ
ব্যবহার করো, অ্যাডমিন রুট গ্রুপে middleware দিয়ে প্রোটেক্ট করো।
```

---

## Phase 10: টেস্টিং ও ডিপ্লয়মেন্ট প্রস্তুতি

```
প্রজেক্টটা প্রোডাকশনের জন্য প্রস্তুত করো:

1. মূল ফ্লোগুলোর জন্য Feature টেস্ট লিখো (Pest):
   - ইউজার প্রজেক্ট বানাতে পারে
   - সিন যোগ করতে পারে
   - রেন্ডার রিকোয়েস্ট পাঠানো হলে ক্রেডিট কমে
   - ক্রেডিট না থাকলে রেন্ডার ব্লক হয়
2. একটা .env.production.example ফাইল বানাও যেখানে প্রোডাকশনের জন্য
   দরকারি সব ভ্যারিয়েবল (QUEUE_CONNECTION=redis, BROADCAST_DRIVER,
   SHOTSTACK_ENV=production ইত্যাদি) লিস্ট করা থাকবে
3. একটা DEPLOYMENT.md বানাও যেখানে সার্ভার রিকোয়ারমেন্ট
   (PHP 8.3+, Redis, Supervisor for queue worker, Horizon config),
   ও ডিপ্লয়মেন্ট স্টেপ বাই স্টেপ লেখা থাকবে
4. queue worker ও Horizon-এর জন্য Supervisor কনফিগ ফাইলের উদাহরণ দাও

সবশেষে একটা README.md বানাও যেখানে পুরো প্রজেক্টের সেটআপ ইন্সট্রাকশন,
.env ভ্যারিয়েবল লিস্ট, ও লোকাল ডেভেলপমেন্ট চালানোর কমান্ড লেখা থাকবে।
```

---

## ব্যবহারের নিয়ম

1. প্রতিটা ফেজ **আলাদাভাবে** Claude Code-এ পেস্ট করুন এবং শেষ হলে টেস্ট করুন
2. Phase 5 এ যাওয়ার আগে **Shotstack/Creatomate-এ ফ্রি একাউন্ট খুলে API key নিয়ে নিন** — এটা কোড শুরুর আগে ম্যানুয়ালি করতে হবে
3. Phase 3-এ ক্যারেক্টার আর্ট এখনো placeholder — আসল কার্টুন ক্যারেক্টার ডিজাইন হয়ে গেলে সেগুলো দিয়ে বদলে দিন
4. প্রতি ফেজ শেষে `git commit` করে রাখুন, যাতে কোনো ফেজে সমস্যা হলে আগের অবস্থায় ফিরতে পারেন
